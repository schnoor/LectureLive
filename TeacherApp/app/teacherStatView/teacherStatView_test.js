'use strict';

describe('myApp.teacherStatView module', function() {

  beforeEach(module('myApp.teacherStatView'));

  describe('teacherStatView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var teacherStatViewCtrl = $controller('teacherStatViewCtrl');
      expect(teacherStatViewCtrl).toBeDefined();
    }));

  });
});
