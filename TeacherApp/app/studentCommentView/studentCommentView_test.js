'use strict';

describe('myApp.studentCommentView module', function() {

  beforeEach(module('myApp.studentCommentView'));

  describe('studentCommentView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var studentCommentViewCtrl = $controller('studentCommentViewCtrl');
      expect(studentCommentViewCtrl).toBeDefined();
    }));

  });
});